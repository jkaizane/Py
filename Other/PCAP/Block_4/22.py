#If you want to check if a Python file is either used as a module or run as a standalone program, you should the built-in variable named:

__name__
"""The special variable __name__ contains the string '__main__' when the code is run as a standalone program, 
or a module name if a Pyhon file is used as a module"""