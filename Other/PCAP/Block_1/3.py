# What is true about the built-in dir() mechanism in the contect of modules and packages?

#It is a function which can be invoked with a module passed as an arguemt in order to obtain module content.
#dir() is obviously a fucntion; when invoked with argument as a module, it returns a list which contains the names of the module attributes.