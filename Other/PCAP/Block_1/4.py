#What is true about sys.path?

#It is a list of strings which indicates all the directories/folders scanned by Python in order to find the specific modules.
"""While sys is a module containing facilities which allow the user to diagnose certain features of the OS enncvironment, 
path is a list of strings that specifies the search path for modules. It is initialised upon program start-up, and path[0]
shows the name of the directory containing the script that has been used to invoke the Python interpreter."""