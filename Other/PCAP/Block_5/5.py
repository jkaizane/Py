#Which of the following lines contain valud Python code?

lambda x: None
#This lambda returns None regardless of its argument's value - not very useful, but correct.

lambda x: x + 1
#This lambda returns its argument incremented by one.